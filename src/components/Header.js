import React from 'react';

import '../styles/Header.scss';

const Header = () => {
  return <header id='picture-element-editor-header'>
    Picture Element Editor (PEE)
  </header>;
};

export default Header;
